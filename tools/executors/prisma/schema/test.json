{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "cwd": {
      "type": "string",
      "description": "path to the project folder"
    },
    "schemaPath": {
      "type": "string",
      "default": "prisma/schema.prisma",
      "description": "path to the prisma schema"
    }
  }
}